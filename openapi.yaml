# Generated by scripts/generate_openapi.py — do not hand-edit.
openapi: 3.1.0
info:
  title: GWEB Merchant Onboarding API
  version: 1.0.0
  description: Generated directly from src/models/*.py — do not hand-edit; run scripts/generate_openapi.py
    after any model change. See README.md for the full contract notes and SECURITY.md for the auth model.
servers:
- url: /v1
paths:
  /applications:
    post:
      summary: Create an application
      tags:
      - Applications
      parameters: []
      responses:
        '201':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateApplicationResponse'
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}:
    get:
      summary: Aggregated read across the whole application
      tags:
      - Applications
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetApplicationResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/applicant:
    patch:
      summary: Upsert one owner/controller
      tags:
      - Applicant
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchApplicantRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatchApplicantResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/business:
    patch:
      summary: Upsert the business profile
      tags:
      - Business
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchBusinessRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatchBusinessResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/classify:
    post:
      summary: Deterministic MCC classification (no AI/LLM)
      tags:
      - MCC
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClassifyRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassifyResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/documents/presign:
    post:
      summary: Get a presigned S3 upload URL
      tags:
      - Documents
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PresignDocumentRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresignDocumentResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/documents/{documentId}/complete:
    post:
      summary: Verify an uploaded document (size/signature/checksum)
      tags:
      - Documents
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: documentId
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompleteDocumentRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompleteDocumentResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/evaluate:
    post:
      summary: Deterministic rate/fee calc + mock AI commentary
      tags:
      - Evaluation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EvaluateRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvaluateResponse'
        '202':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvaluateResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/evaluation:
    get:
      summary: Poll the evaluation result
      tags:
      - Evaluation
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetEvaluationResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/mcc/confirm:
    post:
      summary: Applicant confirms or corrects the MCC
      tags:
      - MCC
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmMccRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfirmMccResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/review:
    get:
      summary: Readiness view — same computation submit validates against
      tags:
      - Review & Submit
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReviewResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /applications/{id}/submit:
    post:
      summary: Validate and submit (DRAFT -> SUBMITTED)
      tags:
      - Review & Submit
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: X-Application-Token
        in: header
        required: true
        description: Must equal the {id} path parameter — see SECURITY.md.
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitRequest'
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitResponse'
        '400':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitResponse'
        '401':
          description: Missing/invalid X-Application-Token
        '404':
          description: Application (or referenced resource) not found
  /mcc:
    get:
      summary: Search the packaged MCC catalog
      tags:
      - MCC
      parameters:
      - name: query
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: See SECURITY.md/README.md for auth/error semantics.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchMccResponse'
        '404':
          description: Application (or referenced resource) not found
components:
  schemas:
    AiCommentaryOutput:
      description: 'Strict schema for AI-adapter output — text only, no numeric fields

        exist on this model, so AI cannot supply a number even by accident.

        Treated as untrusted regardless of which provider produced it; the

        orchestrator always re-validates a raw dict against this schema.'
      properties:
        summary:
          minLength: 1
          title: Summary
          type: string
        highlights:
          items:
            type: string
          title: Highlights
          type: array
      required:
      - summary
      title: AiCommentaryOutput
      type: object
    ApplicationMetaView:
      properties:
        status:
          $ref: '#/components/schemas/ApplicationStatus'
        version:
          title: Version
          type: integer
        createdAt:
          title: Createdat
          type: string
        updatedAt:
          title: Updatedat
          type: string
        applicantComplete:
          title: Applicantcomplete
          type: boolean
        businessComplete:
          title: Businesscomplete
          type: boolean
      required:
      - status
      - version
      - createdAt
      - updatedAt
      - applicantComplete
      - businessComplete
      title: ApplicationMetaView
      type: object
    ApplicationStatus:
      enum:
      - DRAFT
      - SUBMITTED
      title: ApplicationStatus
      type: string
    BusinessActivityView:
      properties:
        selfSelectedActivity:
          title: Selfselectedactivity
          type: string
        businessDescription:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Businessdescription
      required:
      - selfSelectedActivity
      title: BusinessActivityView
      type: object
    BusinessView:
      properties:
        legalName:
          title: Legalname
          type: string
        dba:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Dba
        entityType:
          title: Entitytype
          type: string
        registrationId:
          title: Registrationid
          type: string
        addresses:
          additionalProperties: true
          title: Addresses
          type: object
        volumeMetrics:
          additionalProperties: true
          title: Volumemetrics
          type: object
        existingProcessor:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          default: null
          title: Existingprocessor
      required:
      - legalName
      - entityType
      - registrationId
      - addresses
      - volumeMetrics
      title: BusinessView
      type: object
    CatalogSearchResult:
      description: One row of `GET /mcc?query=`.
      properties:
        code:
          title: Code
          type: string
        description:
          title: Description
          type: string
        category:
          title: Category
          type: string
      required:
      - code
      - description
      - category
      title: CatalogSearchResult
      type: object
    ClassifyRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
        selfSelectedActivity:
          minLength: 1
          title: Selfselectedactivity
          type: string
        businessDescription:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Businessdescription
      required:
      - requestId
      - selfSelectedActivity
      title: ClassifyRequest
      type: object
    ClassifyResponse:
      properties:
        candidates:
          items:
            $ref: '#/components/schemas/MccCandidate'
          title: Candidates
          type: array
        requiresManualReview:
          title: Requiresmanualreview
          type: boolean
      required:
      - requiresManualReview
      title: ClassifyResponse
      type: object
    CompleteDocumentRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
      required:
      - requestId
      title: CompleteDocumentRequest
      type: object
    CompleteDocumentResponse:
      properties:
        documentId:
          title: Documentid
          type: string
        status:
          $ref: '#/components/schemas/DocumentStatus'
        checksumSha256:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Checksumsha256
        rejectionReason:
          anyOf:
          - $ref: '#/components/schemas/RejectionReason'
          - type: 'null'
          default: null
        verifiedAt:
          title: Verifiedat
          type: string
      required:
      - documentId
      - status
      - verifiedAt
      title: CompleteDocumentResponse
      type: object
    ConfirmMccRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
        code:
          minLength: 1
          title: Code
          type: string
        selfSelectedActivity:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Selfselectedactivity
      required:
      - requestId
      - code
      title: ConfirmMccRequest
      type: object
    ConfirmMccResponse:
      properties:
        code:
          title: Code
          type: string
        confirmedAt:
          title: Confirmedat
          type: string
        riskTier:
          $ref: '#/components/schemas/RiskTier'
        riskReason:
          title: Riskreason
          type: string
        requiresManualReview:
          title: Requiresmanualreview
          type: boolean
      required:
      - code
      - confirmedAt
      - riskTier
      - riskReason
      - requiresManualReview
      title: ConfirmMccResponse
      type: object
    CreateApplicationResponse:
      properties:
        applicationId:
          title: Applicationid
          type: string
        status:
          $ref: '#/components/schemas/ApplicationStatus'
        version:
          title: Version
          type: integer
      required:
      - applicationId
      - status
      - version
      title: CreateApplicationResponse
      type: object
    DocumentMimeType:
      enum:
      - application/pdf
      - image/jpeg
      - image/png
      title: DocumentMimeType
      type: string
    DocumentStatus:
      enum:
      - REQUESTED
      - PROCESSING
      - ACCEPTED
      - REJECTED
      title: DocumentStatus
      type: string
    DocumentSummaryView:
      description: No s3Key, no checksum — internal storage details stay internal.
      properties:
        documentId:
          title: Documentid
          type: string
        docType:
          $ref: '#/components/schemas/DocumentType'
        status:
          $ref: '#/components/schemas/DocumentStatus'
        fileName:
          title: Filename
          type: string
        verifiedAt:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Verifiedat
      required:
      - documentId
      - docType
      - status
      - fileName
      title: DocumentSummaryView
      type: object
    DocumentType:
      enum:
      - GOVERNMENT_ID
      - BUSINESS_REGISTRATION
      - BUSINESS_LICENSE
      - BANK_EVIDENCE
      - PROCESSING_STATEMENT
      - ADDITIONAL_EVIDENCE
      title: DocumentType
      type: string
    EvaluateRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
        statementDocId:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Statementdocid
      required:
      - requestId
      title: EvaluateRequest
      type: object
    EvaluateResponse:
      properties:
        evalId:
          title: Evalid
          type: string
        status:
          $ref: '#/components/schemas/EvaluationStatus'
        statementMetrics:
          anyOf:
          - $ref: '#/components/schemas/StatementMetrics'
          - type: 'null'
          default: null
        effectiveRate:
          anyOf:
          - type: number
          - type: 'null'
          default: null
          title: Effectiverate
        aiCommentary:
          anyOf:
          - $ref: '#/components/schemas/AiCommentaryOutput'
          - type: 'null'
          default: null
        riskFlags:
          items:
            $ref: '#/components/schemas/RiskFlag'
          title: Riskflags
          type: array
      required:
      - evalId
      - status
      title: EvaluateResponse
      type: object
    EvaluationStatus:
      enum:
      - PENDING
      - PROCESSING
      - COMPLETE
      - FAILED
      title: EvaluationStatus
      type: string
    GetApplicationResponse:
      description: 'Aggregated read across the whole partition — see spec §3 note on

        `GET /applications/{id}`. `mcc` (Phase 3) and `evaluation` (Phase 4)

        are wired to real data; `documents` stays empty until that phase''s

        aggregate wiring is added.'
      properties:
        meta:
          $ref: '#/components/schemas/ApplicationMetaView'
        applicant:
          items:
            $ref: '#/components/schemas/PersonView'
          title: Applicant
          type: array
        business:
          anyOf:
          - $ref: '#/components/schemas/BusinessView'
          - type: 'null'
          default: null
        documents:
          items:
            additionalProperties: true
            type: object
          title: Documents
          type: array
        mcc:
          $ref: '#/components/schemas/MccView'
        evaluation:
          anyOf:
          - $ref: '#/components/schemas/GetEvaluationResponse'
          - type: 'null'
          default: null
      required:
      - meta
      title: GetApplicationResponse
      type: object
    GetEvaluationResponse:
      description: '`GET /applications/{id}/evaluation` — spec §3 (no evalId in this shape).'
      properties:
        status:
          $ref: '#/components/schemas/EvaluationStatus'
        statementMetrics:
          anyOf:
          - $ref: '#/components/schemas/StatementMetrics'
          - type: 'null'
          default: null
        effectiveRate:
          anyOf:
          - type: number
          - type: 'null'
          default: null
          title: Effectiverate
        aiCommentary:
          anyOf:
          - $ref: '#/components/schemas/AiCommentaryOutput'
          - type: 'null'
          default: null
        riskFlags:
          items:
            $ref: '#/components/schemas/RiskFlag'
          title: Riskflags
          type: array
      required:
      - status
      title: GetEvaluationResponse
      type: object
    MccCandidate:
      description: 'One classification candidate — also the shape stored in

        MCC#PROPOSED''s `candidates` list.'
      properties:
        code:
          title: Code
          type: string
        description:
          title: Description
          type: string
        confidence:
          title: Confidence
          type: number
        reason:
          title: Reason
          type: string
      required:
      - code
      - description
      - confidence
      - reason
      title: MccCandidate
      type: object
    MccConfirmedView:
      description: Nested under `GET /applications/{id}`'s `mcc.confirmed`.
      properties:
        code:
          title: Code
          type: string
        confirmedAt:
          title: Confirmedat
          type: string
        selfSelectedActivity:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Selfselectedactivity
        riskTier:
          $ref: '#/components/schemas/RiskTier'
        riskReason:
          title: Riskreason
          type: string
        provider:
          default: default
          title: Provider
          type: string
      required:
      - code
      - confirmedAt
      - riskTier
      - riskReason
      title: MccConfirmedView
      type: object
    MccProposedView:
      description: Nested under `GET /applications/{id}`'s `mcc.proposed`.
      properties:
        candidates:
          items:
            $ref: '#/components/schemas/MccCandidate'
          title: Candidates
          type: array
        requiresManualReview:
          title: Requiresmanualreview
          type: boolean
        generatedAt:
          title: Generatedat
          type: string
        selfSelectedActivity:
          title: Selfselectedactivity
          type: string
        businessDescription:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Businessdescription
      required:
      - requiresManualReview
      - generatedAt
      - selfSelectedActivity
      title: MccProposedView
      type: object
    MccView:
      properties:
        proposed:
          anyOf:
          - $ref: '#/components/schemas/MccProposedView'
          - type: 'null'
          default: null
        confirmed:
          anyOf:
          - $ref: '#/components/schemas/MccConfirmedView'
          - type: 'null'
          default: null
      title: MccView
      type: object
    PatchApplicantRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
        personId:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Personid
        firstName:
          minLength: 1
          title: Firstname
          type: string
        lastName:
          minLength: 1
          title: Lastname
          type: string
        dob:
          title: Dob
          type: string
        address:
          additionalProperties: true
          title: Address
          type: object
        role:
          minLength: 1
          title: Role
          type: string
        ownershipPct:
          anyOf:
          - maximum: 100
            minimum: 0
            type: number
          - type: 'null'
          default: null
          title: Ownershippct
        idType:
          minLength: 1
          title: Idtype
          type: string
        idLast4:
          maxLength: 4
          minLength: 4
          title: Idlast4
          type: string
        consentVersion:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Consentversion
      required:
      - requestId
      - firstName
      - lastName
      - dob
      - address
      - role
      - idType
      - idLast4
      title: PatchApplicantRequest
      type: object
    PatchApplicantResponse:
      properties:
        personId:
          title: Personid
          type: string
        version:
          title: Version
          type: integer
      required:
      - personId
      - version
      title: PatchApplicantResponse
      type: object
    PatchBusinessRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
        legalName:
          minLength: 1
          title: Legalname
          type: string
        dba:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Dba
        entityType:
          minLength: 1
          title: Entitytype
          type: string
        registrationId:
          minLength: 1
          title: Registrationid
          type: string
        addresses:
          additionalProperties: true
          title: Addresses
          type: object
        volumeMetrics:
          additionalProperties: true
          title: Volumemetrics
          type: object
        existingProcessor:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          default: null
          title: Existingprocessor
      required:
      - requestId
      - legalName
      - entityType
      - registrationId
      - addresses
      - volumeMetrics
      title: PatchBusinessRequest
      type: object
    PatchBusinessResponse:
      properties:
        version:
          title: Version
          type: integer
      required:
      - version
      title: PatchBusinessResponse
      type: object
    PersonView:
      properties:
        personId:
          title: Personid
          type: string
        role:
          title: Role
          type: string
        ownershipPct:
          anyOf:
          - type: number
          - type: 'null'
          default: null
          title: Ownershippct
        firstName:
          title: Firstname
          type: string
        lastName:
          title: Lastname
          type: string
        dob:
          title: Dob
          type: string
        address:
          additionalProperties: true
          title: Address
          type: object
        idType:
          title: Idtype
          type: string
        idLast4:
          title: Idlast4
          type: string
        consentTimestamp:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Consenttimestamp
        consentVersion:
          anyOf:
          - type: string
          - type: 'null'
          default: null
          title: Consentversion
      required:
      - personId
      - role
      - firstName
      - lastName
      - dob
      - address
      - idType
      - idLast4
      title: PersonView
      type: object
    PresignDocumentRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
        docType:
          $ref: '#/components/schemas/DocumentType'
        fileName:
          title: Filename
          type: string
        mimeType:
          $ref: '#/components/schemas/DocumentMimeType'
        sizeBytes:
          exclusiveMinimum: 0
          maximum: 15728640
          title: Sizebytes
          type: integer
      required:
      - requestId
      - docType
      - fileName
      - mimeType
      - sizeBytes
      title: PresignDocumentRequest
      type: object
    PresignDocumentResponse:
      properties:
        documentId:
          title: Documentid
          type: string
        uploadUrl:
          title: Uploadurl
          type: string
        s3Key:
          title: S3Key
          type: string
        expiresIn:
          title: Expiresin
          type: integer
      required:
      - documentId
      - uploadUrl
      - s3Key
      - expiresIn
      title: PresignDocumentResponse
      type: object
    RejectionReason:
      description: Safe, non-sensitive codes — never derived from file contents.
      enum:
      - object_not_found
      - file_too_large
      - size_mismatch
      - signature_mismatch
      title: RejectionReason
      type: string
    ReviewResponse:
      description: '`GET /applications/{id}/review` — and the shape of `submit`''s

        `reviewPayload` on success. Client-facing: no secrets, no document

        contents, no S3 references.'
      properties:
        applicationStatus:
          $ref: '#/components/schemas/ApplicationStatus'
        applicant:
          items:
            $ref: '#/components/schemas/PersonView'
          title: Applicant
          type: array
        business:
          anyOf:
          - $ref: '#/components/schemas/BusinessView'
          - type: 'null'
          default: null
        documents:
          items:
            $ref: '#/components/schemas/DocumentSummaryView'
          title: Documents
          type: array
        missingDocumentTypes:
          items:
            $ref: '#/components/schemas/DocumentType'
          title: Missingdocumenttypes
          type: array
        businessActivity:
          anyOf:
          - $ref: '#/components/schemas/BusinessActivityView'
          - type: 'null'
          default: null
        mcc:
          $ref: '#/components/schemas/MccView'
        riskTier:
          $ref: '#/components/schemas/RiskTier'
          default: STANDARD
        requiresManualReview:
          default: false
          title: Requiresmanualreview
          type: boolean
        evaluation:
          anyOf:
          - $ref: '#/components/schemas/GetEvaluationResponse'
          - type: 'null'
          default: null
        warnings:
          items:
            type: string
          title: Warnings
          type: array
        blockingIssues:
          items:
            $ref: '#/components/schemas/ValidationIssue'
          title: Blockingissues
          type: array
        ready:
          default: false
          title: Ready
          type: boolean
      required:
      - applicationStatus
      title: ReviewResponse
      type: object
    RiskFlag:
      description: 'Deterministic only — see risk_flag_service.py. `field` is always a

        real dot-path into the application data that triggered the flag.'
      properties:
        field:
          title: Field
          type: string
        message:
          title: Message
          type: string
        severity:
          $ref: '#/components/schemas/RiskSeverity'
      required:
      - field
      - message
      - severity
      title: RiskFlag
      type: object
    RiskSeverity:
      enum:
      - LOW
      - MEDIUM
      - HIGH
      title: RiskSeverity
      type: string
    RiskTier:
      enum:
      - STANDARD
      - ENHANCED_REVIEW
      - RESTRICTED
      title: RiskTier
      type: string
    SearchMccResponse:
      properties:
        results:
          items:
            $ref: '#/components/schemas/CatalogSearchResult'
          title: Results
          type: array
      title: SearchMccResponse
      type: object
    StatementMetrics:
      description: 'Derived from BUSINESS.volumeMetrics (Phase 1 data) — no OCR, no

        parsing of statementDocId''s actual bytes.'
      properties:
        monthlyVolume:
          title: Monthlyvolume
          type: number
        averageTicket:
          anyOf:
          - type: number
          - type: 'null'
          default: null
          title: Averageticket
        currentProcessingRate:
          anyOf:
          - type: number
          - type: 'null'
          default: null
          title: Currentprocessingrate
        transactionCount:
          anyOf:
          - type: integer
          - type: 'null'
          default: null
          title: Transactioncount
        perTransactionFee:
          anyOf:
          - type: number
          - type: 'null'
          default: null
          title: Pertransactionfee
      required:
      - monthlyVolume
      title: StatementMetrics
      type: object
    SubmitRequest:
      properties:
        requestId:
          minLength: 1
          title: Requestid
          type: string
      required:
      - requestId
      title: SubmitRequest
      type: object
    SubmitResponse:
      description: '`POST /applications/{id}/submit` — spec §3: `200 { status:

        "SUBMITTED", reviewPayload }` or `400 { missingItems: [...] }`.'
      properties:
        status:
          $ref: '#/components/schemas/ApplicationStatus'
        reviewPayload:
          anyOf:
          - $ref: '#/components/schemas/ReviewResponse'
          - type: 'null'
          default: null
        missingItems:
          items:
            $ref: '#/components/schemas/ValidationIssue'
          title: Missingitems
          type: array
      required:
      - status
      title: SubmitResponse
      type: object
    ValidationIssue:
      properties:
        code:
          title: Code
          type: string
        message:
          title: Message
          type: string
        field:
          title: Field
          type: string
      required:
      - code
      - message
      - field
      title: ValidationIssue
      type: object
